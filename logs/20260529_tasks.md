# 업무 보고서 — 2026-05-29

## 작업: Render 배포 1/4 — 운영환경 패키지 설치 + settings.py 배포 대응

### 1. 패키지 설치
- `psycopg2-binary==2.9.12` 설치 (PostgreSQL 드라이버)
- `dj-database-url==3.1.2` 설치 (DATABASE_URL 파싱)
- gunicorn 26.0.0 / whitenoise 6.12.0 은 기존 설치분 재사용 (Railway 설정 시 추가됨)

### 2. requirements.txt
- 위 2개 패키지 직접 추가 (기존 직접관리 스타일 유지, pip freeze 전체 덮어쓰기 미적용)

### 3. config/settings.py 수정
| 조건 | 처리 |
|------|------|
| ① os/dj_database_url import | dj_database_url import 추가 (os는 기존 존재) |
| ② SECRET_KEY 환경변수 | 기존 충족 — 변경 없음 |
| ③ DEBUG = 'RENDER' in os.environ | `DEBUG = 'RENDER' not in os.environ` 로 변경 |
| ④ ALLOWED_HOSTS Render 도메인 | RENDER_EXTERNAL_HOSTNAME 자동 허용 + CSRF에 *.onrender.com 추가 |
| ⑤ DATABASES 조건분기 | DATABASE_URL 있으면 PostgreSQL, 없으면 기존 SQLite(WAL PRAGMA 보존) |
| ⑥ whitenoise 미들웨어 | 기존 충족 — 변경 없음 |
| ⑦ STATIC_ROOT/STATICFILES_STORAGE | 기존 충족 — 변경 없음 |

### 4. 검증
- `manage.py check` → System check identified no issues (0 silenced)
- 로컬 환경: DB 엔진 sqlite3, DEBUG=True 정상 (운영 설정이 로컬을 깨지 않음 확인)

### 비고
- Railway 설정(Procfile, runtime.txt)이 잔존 — Render 배포 시 render.yaml이 우선이며 무해하나, 혼란 방지를 위해 다음 단계에서 정리 검토 필요
- 다음: 2/4 render.yaml 생성

---

## 작업: Render 배포 2/4 — render.yaml 생성 + 배포 전 검증 + 권고 반영

### 1. render.yaml 생성
- databases: community-db (PostgreSQL, plan free)
- services: community-web (web, python, plan free)
  - buildCommand: pip install + collectstatic + migrate
  - startCommand: gunicorn config.wsgi
  - envVars: PYTHON_VERSION=3.12.0, DATABASE_URL(fromDatabase), SECRET_KEY(generateValue)

### 2. 배포 전 검증 (병렬 워크플로우 3종, 55초)
- 운영 보안 / render.yaml 정합성 / 빌드 드라이런 → 종합 WARN, BLOCK 0건
- check --deploy: ERRORS 0, WARNINGS 6 / collectstatic dry-run 정상 / 미반영 마이그레이션 없음

### 3. 권고 반영
| 항목 | 처리 |
|------|------|
| 보안 헤더 누락 | settings.py: not DEBUG 분기에 SECURE_SSL_REDIRECT/HSTS/SESSION·CSRF_COOKIE_SECURE/SECURE_PROXY_SSL_HEADER 추가 |
| ngrok 와일드카드 운영 노출 | ALLOWED_HOSTS/CSRF의 ngrok 도메인을 DEBUG일 때만 포함하도록 분기 |
| runtime.txt 무효 | 삭제 + render.yaml PYTHON_VERSION으로 대체 |
| Procfile 중복 | 삭제 (render.yaml startCommand로 단일화) |

### 4. 검증 결과
- 로컬 check: 0 issues (DEBUG=True, SQLite 유지)
- 운영 시뮬(RENDER=true) check --deploy: 경고 6 → 1 (W009 SECRET_KEY만 잔존, Render generateValue가 해소)

### 다음: 3/4 git commit + push → Render 대시보드 Blueprint 연결
